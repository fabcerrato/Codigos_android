//previamente debemos de crear una clase en nuestro proyecto el cual contendrá los mismos campos que extraeremos de la base de datos

//El código para extraer los datos de firebase firestore es el siguiente

        //Esta lista es por si queremos mostrar los datos extraidos de firebase
        ListView Lista;


        CollectionReference RefColeccion = FirebaseFirestore.getInstance().collection("Mi_coleccion");

        RefColeccion.get().addOnSuccessListener(new OnSuccessListener<QuerySnapshot>(){
            @Override
            public void onSuccess(QuerySnapshot queryDocumentSnapshots) {

                ArrayList<String> lista = new ArrayList<>();

                if (queryDocumentSnapshots.isEmpty()) {
                    Toast.makeText(MainActivity.this, "No se leyeron los datos", Toast.LENGTH_SHORT).show();
                } else {
                    for (QueryDocumentSnapshot documentSnapshot : queryDocumentSnapshots) {

                        //Guardamos los datos del documento actual en la clase User
                        User listaRecibida = documentSnapshot.toObject(User.class);

                       //Por ejemplo: Si desea almacenar los datos de la clase User en un ArrayList para mostrarlos al final del ciclo
                        String data = "";
                        data = listaRecibida.getUsuario() + " " + listaRecibida.getApellido() + " " + listaRecibida.getCorreo();
                        lista.add(data);

                    }
                    ArrayAdapter<String> adapter = new ArrayAdapter<String>(MainActivity.this, android.R.layout.simple_list_item_1, lista);
                    MyAdapter myAdapter = new MyAdapter(MainActivity.this, R.layout.list_item, lista);
                    Lista.setAdapter(myAdapter);

                }//FIN ELSE
            }//FIN onSuccess
        });